datasource db {
  provider = "sqlite"
  url = "file:./dev.db"
}

model Link {
  id        Int         @id  @default(autoincrement())
  original  String
  short     String      @unique
  createdAt DateTime    @default(now())
}